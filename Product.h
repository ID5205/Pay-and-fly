#ifndef PRODUCT
#define PRODUCT

class Product
{
public:
    float height;
    float width;
    float length;
    float weight;
    Product();
	Product(float new_height, float new_width, float new_length, float new_weight);
};

#endif //PRODUCT

